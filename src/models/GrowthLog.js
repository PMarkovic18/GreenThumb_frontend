class GrowthLog {
    constructor({ id, plantID, date, height, note }) {
        this.id = id
        this.plantID = plantID
        this.date = date
        this.height = height
        this.note = note
    }
}

export default GrowthLog
